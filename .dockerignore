# Git
.git
.gitignore

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
venv/
.env

# Project specific - runtime data (large, regenerated at runtime)
data/
# NOTE: models/ is intentionally NOT excluded here.
# On localhost, docker-compose volume-mounts ./models:/app/models so it doesn't matter.
# On HF Spaces there are no volume mounts - models must be baked in via COPY . .
# audit/ and reports/ are also included so pre-seeded history is available.
frontend/node_modules/
frontend/dist/
frontend/src/
*.log

# Tests
.pytest_cache/
.tox/
.coverage
htmlcov/
